# BidX Backend

This project contains a starter FastAPI backend structure for the BidX application.

## Quick start

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the API:
   ```bash
   uvicorn app.main:app --reload
   ```
3. Open http://127.0.0.1:8000/health
